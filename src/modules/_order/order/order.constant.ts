export enum OrderType {
    standard = 'standard',
    premium = 'premium', 
    vip = 'vip',
}

export enum OrderStatus {
    pending = 'pending',
    processing = 'processing',
    complete = 'complete', 
    failed = 'failed',
    refunded = 'refunded',
}
